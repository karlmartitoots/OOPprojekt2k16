package collection;


import card.Minion;

public class SampleMinion extends Minion{

    public SampleMinion(String name, int cost, String effect, int ID, int attack, int maxHp, int speed) {
        super(name, cost, effect, ID, attack, maxHp, speed);
    }
}
